// TODO: Add followers to favorites
import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Favorites = () => (
  <SafeAreaView>
    <Text>Favorites Screen</Text>
  </SafeAreaView>
);

export default Favorites;
